# Prebuilt Binary Placeholder

This directory will contain the prebuilt `zigpug.node` binary for this platform.

The binary will be downloaded from GitHub Actions artifacts after the workflow completes.
